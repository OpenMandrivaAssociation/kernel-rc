diff -up linux-7.2-rc6/tools/build/feature/test-libperl.c.omv~ linux-7.2-rc6/tools/build/feature/test-libperl.c
--- linux-7.2-rc6/tools/build/feature/test-libperl.c.omv~	2026-08-03 01:24:24.000000000 +0200
+++ linux-7.2-rc6/tools/build/feature/test-libperl.c	2026-08-09 23:24:11.124168805 +0200
@@ -1,4 +1,5 @@
 // SPDX-License-Identifier: GPL-2.0
+#pragma GCC diagnostic ignored "-Wcompound-token-split-by-macro"
 #include <EXTERN.h>
 #include <perl.h>
 
diff -up linux-7.2-rc6/tools/perf/Makefile.config.omv~ linux-7.2-rc6/tools/perf/Makefile.config
--- linux-7.2-rc6/tools/perf/Makefile.config.omv~	2026-08-03 01:24:24.000000000 +0200
+++ linux-7.2-rc6/tools/perf/Makefile.config	2026-08-09 23:24:11.157396270 +0200
@@ -127,6 +127,10 @@ endif
 LIBUNWIND_ARCHS:=aarch64 arm loongarch64 mips ppc32 ppc64 riscv s390x x86 x86_64
 
 # "Local" (no arch specified) feature test flags.
+ifndef LIBUNWIND_DIR
+  LIBUNWIND_CFLAGS  ?= -I/usr/include/libunwind
+  LIBUNWIND_LDFLAGS ?= -L/usr/lib64
+endif
 FEATURE_CHECK_CFLAGS-libunwind = $(LIBUNWIND_CFLAGS)
 FEATURE_CHECK_LDFLAGS-libunwind = $(LIBUNWIND_LDFLAGS) $(LIBUNWIND_LIBS)
 FEATURE_CHECK_CFLAGS-libunwind-debug-frame = $(LIBUNWIND_CFLAGS)
@@ -1067,11 +1071,15 @@ ifdef EXTRA_TESTS
 endif
 
 ifndef NO_JVMTI
-  ifneq (,$(wildcard /usr/sbin/update-java-alternatives))
-    JDIR=$(shell /usr/sbin/update-java-alternatives -l | head -1 | awk '{print $$3}')
+  ifneq (,$(JAVA_HOME))
+    JDIR=$(JAVA_HOME)
   else
-    ifneq (,$(wildcard /usr/sbin/alternatives))
-      JDIR=$(shell /usr/sbin/alternatives --display java | tail -1 | cut -d' ' -f 5 | sed -e 's%/jre/bin/java.%%g' -e 's%/bin/java.%%g')
+    ifneq (,$(wildcard /usr/sbin/update-java-alternatives))
+      JDIR=$(shell /usr/sbin/update-java-alternatives -l | head -1 | awk '{print $$3}')
+    else
+      ifneq (,$(wildcard /usr/sbin/alternatives))
+        JDIR=$(shell /usr/sbin/alternatives --display java | tail -1 | cut -d' ' -f 5 | sed -e 's%/jre/bin/java.%%g' -e 's%/bin/java.%%g')
+      endif
     endif
   endif
   ifndef JDIR
